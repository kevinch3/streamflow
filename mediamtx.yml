logLevel: info

# Authentication — open publish for local dev (no RTMP credentials required)
# For production: replace with a named publisher user + RTMP_PUBLISH_KEY (see readme)
authInternalUsers:
  - user: any
    pass:
    permissions:
      - action: api
      - action: publish
      - action: read
      - action: playback

# Disable unused protocols
rtsp: no
webrtc: no
srt: no

# REST API (internal — only Express accesses this)
api: yes
apiAddress: :9997

# RTMP ingest
rtmp: yes
rtmpAddress: :1935

# HLS output
hls: yes
hlsAddress: :8888
hlsVariant: mpegts
hlsAlwaysRemux: yes
hlsSegmentDuration: 2s
hlsSegmentCount: 3
hlsAllowOrigins:
  - '*'

# Accept any stream path (wildcard)
paths:
  all_others:
